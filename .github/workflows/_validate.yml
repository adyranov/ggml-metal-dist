name: Validate
on:
  workflow_call:
    inputs:
      tier:
        required: true
        type: string
        description: "Model tier (smoke or full)"
      matrix:
        required: true
        type: string
        description: "Validation matrix JSON"
      brew_deps:
        required: true
        type: string
        description: "Homebrew dependencies"
      ggml_head:
        required: false
        type: string
        description: "Expected reconciled ggml HEAD"
        default: ""
permissions:
  contents: read
jobs:
  validate:
    timeout-minutes: ${{ matrix.type == 'unit' && 20 || matrix.type == 'performance' && 90 || 60 }}
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(inputs.matrix) }}
    runs-on: ${{ matrix.runs-on }}
    env:
      HF_CACHE_KEY_PREFIX: hf-models-${{ inputs.tier }}-${{ matrix.tool }}
      HF_HOME: ${{ github.workspace }}/.hf-cache
      HF_HUB_CACHE: ${{ github.workspace }}/.hf-cache/hub
      HUGGINGFACE_HUB_CACHE: ${{ github.workspace }}/.hf-cache/hub
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false
      - id: setup_build_env
        uses: ./.github/actions/setup-build-env
        with:
          brew_deps: ${{ inputs.brew_deps }}
          runs_on: ${{ matrix.runs-on }}
          tool: ${{ matrix.tool }}
          enable_ccache: ${{ matrix.type == 'unit' || matrix.type == 'build' }}
          cache_prefix: ""
      - name: Setup mise
        uses: jdx/mise-action@dba19683ed58901619b14f395a24841710cb4925 # v4.1.0
      - name: Restore Hugging Face models cache
        if: matrix.type != 'unit'
        uses: actions/cache/restore@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: .hf-cache/hub
          key: ${{ env.HF_CACHE_KEY_PREFIX }}-${{ hashFiles('manifest.json') }}
          restore-keys: |
            ${{ env.HF_CACHE_KEY_PREFIX }}-
          enableCrossOsArchive: true
      - name: Verify Hugging Face models cache
        if: matrix.type != 'unit'
        env:
          TIER: ${{ inputs.tier }}
          TOOL: ${{ matrix.tool }}
        run: |
          set -euo pipefail
          ./scripts/dist.sh verify-hf-cache "$TOOL" --tier "$TIER"
      - name: Validate
        env:
          EXPECTED_GGML_HEAD: ${{ inputs.ggml_head }}
          TIER: ${{ inputs.tier }}
          TOOL: ${{ matrix.tool }}
          TYPE: ${{ matrix.type }}
        run: |
          set -euo pipefail
          args=(validate "$TOOL" --type "$TYPE" --tier "$TIER" --verbose)
          [ -z "$EXPECTED_GGML_HEAD" ] || args+=(--expected-ggml-head "$EXPECTED_GGML_HEAD")
          ./scripts/dist.sh "${args[@]}"
          ccache --show-stats || true
      - name: Save ccache
        if: (matrix.type == 'unit' || matrix.type == 'build') && always() && steps.setup_build_env.outputs.ccache_hit != 'true'
        continue-on-error: true
        uses: actions/cache/save@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ${{ github.workspace }}/.ccache
          key: ccache-${{ matrix.tool }}-${{ runner.os }}-${{ runner.arch }}-${{ github.sha }}
