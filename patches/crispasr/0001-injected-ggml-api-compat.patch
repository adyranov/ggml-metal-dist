From 2f2e74950f465abda3c902ab92dc9d0997614d22 Mon Sep 17 00:00:00 2001
From: Artem Dyranov <1038141+adyranov@users.noreply.github.com>
Date: Tue, 7 Jul 2026 09:20:40 -0400
Subject: [PATCH] compat: injected ggml API shims

---
 examples/talk-llama/models/delta-net-base.cpp |  2 +-
 src/core/fastconformer.h                      |  1 +
 src/core/ggml_crisp_compat.h                  | 46 +++++++++++++++++++
 src/core/sanm.h                               |  1 +
 4 files changed, 49 insertions(+), 1 deletion(-)
 create mode 100644 src/core/ggml_crisp_compat.h

diff --git a/examples/talk-llama/models/delta-net-base.cpp b/examples/talk-llama/models/delta-net-base.cpp
index df5e52c..d8c5a88 100755
--- a/examples/talk-llama/models/delta-net-base.cpp
+++ b/examples/talk-llama/models/delta-net-base.cpp
@@ -383,7 +383,7 @@ std::pair<ggml_tensor*, ggml_tensor*> llm_build_delta_net_base::build_delta_net_
     GGML_ASSERT(b->ne[0] == 1 && b->ne[1] == H_v && b->ne[2] == n_tokens && b->ne[3] == n_seqs);
     GGML_ASSERT(s->ne[0] == S_v && s->ne[1] == S_v && s->ne[2] == H_v && s->ne[3] == n_seqs);

-    ggml_tensor* result = ggml_gated_delta_net(ctx0, q, k, v, g, b, s);
+    ggml_tensor* result = ggml_gated_delta_net(ctx0, q, k, v, g, b, s, 1);
     if (n_tokens == 1) {
         cb(result, LLAMA_TENSOR_NAME_FGDN_AR, il);
     } else {
diff --git a/src/core/fastconformer.h b/src/core/fastconformer.h
index 8d62746..17d27c2 100755
--- a/src/core/fastconformer.h
+++ b/src/core/fastconformer.h
@@ -25,6 +25,7 @@
 #pragma once

 #include "ggml.h"
+#include "ggml_crisp_compat.h"

 #include <cmath>
 #include <cstddef>
diff --git a/src/core/ggml_crisp_compat.h b/src/core/ggml_crisp_compat.h
new file mode 100644
index 0000000..338c2b1
--- /dev/null
+++ b/src/core/ggml_crisp_compat.h
@@ -0,0 +1,46 @@
+// Compat shims for CrispASR ops absent from injected adyranov/ggml.
+#pragma once
+
+#include "ggml.h"
+
+#ifdef __cplusplus
+extern "C" {
+#endif
+
+static inline struct ggml_tensor * ggml_norm_affine(
+        struct ggml_context * ctx,
+        struct ggml_tensor  * a,
+        struct ggml_tensor  * w,
+        struct ggml_tensor  * b,
+        float                 eps) {
+    return ggml_add(ctx, ggml_mul(ctx, ggml_norm(ctx, a, eps), w), b);
+}
+
+static inline struct ggml_tensor * ggml_crisp_view_gate_half(
+        struct ggml_context * ctx,
+        struct ggml_tensor  * a,
+        bool                  second_half) {
+    const int64_t n = a->ne[0] / 2;
+    const size_t  off = second_half ? (size_t) n * a->nb[0] : 0;
+    return ggml_view_2d(ctx, a, n, a->ne[1], a->nb[1], off);
+}
+
+static inline struct ggml_tensor * ggml_siglu(
+        struct ggml_context * ctx,
+        struct ggml_tensor  * a) {
+    struct ggml_tensor * gate = ggml_crisp_view_gate_half(ctx, a, false);
+    struct ggml_tensor * val  = ggml_crisp_view_gate_half(ctx, a, true);
+    return ggml_mul(ctx, ggml_sigmoid(ctx, gate), val);
+}
+
+static inline struct ggml_tensor * ggml_siglu_swapped(
+        struct ggml_context * ctx,
+        struct ggml_tensor  * a) {
+    struct ggml_tensor * gate = ggml_crisp_view_gate_half(ctx, a, false);
+    struct ggml_tensor * val  = ggml_crisp_view_gate_half(ctx, a, true);
+    return ggml_mul(ctx, gate, ggml_sigmoid(ctx, val));
+}
+
+#ifdef __cplusplus
+}
+#endif
diff --git a/src/core/sanm.h b/src/core/sanm.h
index e0fb0a0..2b4dd05 100755
--- a/src/core/sanm.h
+++ b/src/core/sanm.h
@@ -46,6 +46,7 @@
 #pragma once

 #include "ggml.h"
+#include "ggml_crisp_compat.h"

 #include <cmath>
 #include <cstddef>
--
2.55.0
